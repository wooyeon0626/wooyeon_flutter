import 'package:flutter/material.dart';

class LikeTo extends StatelessWidget {
  const LikeTo({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("내가 보낸 좋아요"),);
  }

}