import 'package:flutter/material.dart';

class Matched extends StatelessWidget {
  const Matched({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("매치된 친구"),);
  }

}